# Espressif builds libcxx with pthreads.
# But for tests under QEMU simple pthread stubs are used for single-thread tests only.
if "LIBCXX-ESP-FIXME" in config.available_features:
    config.unsupported = True
